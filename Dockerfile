# Root Dockerfile Placeholder
# This is a placeholder for multi-stage builds or orchestration
# For backend-specific Dockerfile, see ./backend/Dockerfile
# For web-specific Dockerfile, see ./web/Dockerfile (if needed)

# Usage:
# docker build -t zerotrace:latest .
# docker run -p 5000:5000 zerotrace:latest
